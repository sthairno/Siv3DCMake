{
    "version": "0.2.0",
    "inputs": [
        {
            "id": "siv3dAppPath",
            "type": "command",
            "command": "cmake.launchTargetPath",
            "args": {
                "targetName": "Siv3DApp"
            }
        }
    ],
    "configurations": [
        {
            "name": "Debug Siv3DApp",
            "type": "lldb",
            "request": "launch",
            "program": "${input:siv3dAppPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated"
        }
    ]
}
